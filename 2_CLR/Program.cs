using System;

class Progrmam
{
    public static void Main()
    {
        Console.WriteLine("1 => In dot net framework language compiler compile it's code and convert into CIL");
        Console.WriteLine("2 => After that CIL(common intermediate language) (MSIL) (Assembely Language)");
        Console.WriteLine("3 => After that CLR's  JIT Compiler convert native code to machine understandable format");
    }
}